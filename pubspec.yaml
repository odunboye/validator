name: validator
description: A starting point for Dart libraries or applications.
version: 1.0.0
repository: https://github.com/odunboye/validator

environment:
  sdk: ^3.2.4

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  intl: ^0.19.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0
